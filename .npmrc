# pnpm configuration
# Note: Build scripts are currently being skipped for security (pnpm v10+ default)
# The following packages use prebuilt binaries and work without build scripts:
# - @prisma/engines, prisma, bcrypt, better-sqlite3, esbuild
# If you need to run build scripts, use: pnpm approve-builds
